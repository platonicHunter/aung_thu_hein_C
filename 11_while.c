#include <stdio.h>

int main()
{
    // int count = 0;
    // while (count < 101)
    // {
    //     printf("%d\n", count);
    //     count += 10;
    // }

    // 3 * 1=3
    // 3 * 2=6
    // 3 * 3=9
    // 3 * 4=12
    // 3 * 5=15
    // int fixmultiple = 12, count = 1;

    // while (count < 13)
    // {
    //     printf("%d * %d = %d\n", fixmultiple, count, count * fixmultiple);
    //     count++;
    // }

    int num = 1;
    while (num < 9)
    {
        printf("%d Hello World\n", num);
        num++;
    }

    while (num <= 8)
    {
        printf("%d Hello World\n", num);
        num++;
    }

        return 0;
}
