#include <stdio.h>
#include <stdbool.h>

int main()
{
    bool attendence = true;
    /* code */
    int mark = 60;
    int x = 10;
    int y = 9;
    printf("%d", attendence && (mark >= 60));
    return 0;
}
